server:
  port: 91
  context-path: /
spring:
  application:
    name: hystrix-dashboard-turbine
eureka:
  client:
    service-url:
      defaultZone: http://192.168.88.104:3001/eureka/,http://192.168.88.106:3001/eureka/
turbine:
  app-config: student-hystrix-provider
  cluster-name-expression: "'default'"
  #combine-host-port: true
info:
  groupId: ${project.groupId}
  artifactId: ${project.artifactId}
  version: ${project.version}
  provider: turbine_${server.port}
  number: 15282648585
management:
  security:
    enabled: false